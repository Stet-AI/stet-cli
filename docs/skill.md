---
name: Stet docs router
description: Contextual Stet documentation for agents; not the operating contract.
---

This is a documentation router, not Stet's operating contract. Install the canonical Stet skill instead:

```sh
npx skills add https://github.com/Stet-AI/stet-cli.git --skill stet --all
```

Use these docs, the docs MCP, and `llms.txt` as current contextual references.
Do not infer Stet command forms or operating policy from this router; use the
canonical installed skill and its linked guidance.
